#!/usr/bin/env bash
# Badger fast test — runs tests on changed test files only
set -euo pipefail

# Files are passed as arguments
if [ $# -eq 0 ]; then
    echo "No test files to run"
    exit 0
fi

echo "Running tests for $# file(s)..."
bun test "$@"
echo "Tests passed"